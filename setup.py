from distutils.core import setup

setup(name='mete',
      version='1.x-dev',
      py_modules=['mete'])

setup(name='mete_distributions',
      version='1.x-dev',
      py_modules=['mete_distributions'])